# Core runtime and tooling. This list is deliberately small: the system has to
# install and run from a clean checkout in a couple of minutes, and every
# dependency here is imported somewhere in src/, scripts/ or tests/.
#
# The optional dense-retrieval stack (sentence-transformers, chromadb) lives in
# requirements-dense.txt. It is about 800MB and is only needed to reproduce the
# retrieval comparison in ADR-002, so it is not part of running the system.

fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
requests==2.32.3
prometheus-client==0.21.1
numpy==2.1.3

pytest==8.3.4
pytest-cov==6.0.0
# Starlette's TestClient is built on httpx, and without it tests/test_api.py
# skips the whole module rather than failing. A clean checkout was quietly
# running 289 tests and reporting success while the nine that exercise the API
# never ran, including the one that proves an unauthenticated caller cannot
# halt automation. A skipped test that looks like a pass is worse than a
# missing one.
httpx==0.28.1
flake8==7.1.1
black==24.10.0
